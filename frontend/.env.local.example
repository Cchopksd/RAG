# Optional: change this when FastAPI is not running on localhost:8000.
RAG_API_URL=http://localhost:8000

# Server-only scope used to isolate persisted chat history in local deployments.
ATLAS_WORKSPACE_ID=local
